[TOC]

<hr>
11, 30만 수정해서 냈어여><




# 퀴즈

### 1. 리눅스 운영체제에 대한 설명으로 틀린 것은?

```
프로그래밍에 유용하게 사용할 수 있다.
UNIX 계열 운영체제이다.
클라우드 컴퓨팅에서도 많이 사용된다.
Microsoft에서 개발되었다.
```

> 4

<br>

### 2. 다음 중 리눅스 운영체제가 아닌 것은?

```
Ubuntu
Red Hat
Windows 10
CentOS
```

> 3

<br>

### 3. 다음 중 리눅스 운영체제의 특징이 아닌 것은?

```
전역 네임스페이스를 사용한다.
inode값으로 파일을 관리한다.
가상 메모리를 지원하지 않는다.
각 프로세스는 pid값으로 구분한다.
```

> 3



<br>

### 4. 다음 중 Shell의 종류로 알맞지 않은 것은?

```
bash
ksh
zsh
qash
```

> 4



<br>

### 5. 다음 중 리눅스 패키지 설치를 위한 명령어로 알맞은 것은?

```
su
apt-get
passwd
useradd
```

> 2



<br>

### 6. 다음 중 현재 디렉토리에서 파일 목록을 출력하는 shell 명령어로 알맞은 것은?

```
cd
ls
pwd
man
```

> 2

<br>

### 7. 다음 중 특정 파일이 소유자, 그룹, 기타사용자에게 모든 권한을 부여받은 경우의 출력으로 알맞은 것은?

```
- rwx rw- r-x
- rwx rw- rwx
- rwx rwx rwx
- r-- r-- r--
```

> 3



<br>

### 8. 다음 중 fast.c 파일에 사용자와 그룹에게 읽기, 쓰기 권한을 추가하는 명령어로 알맞은 것은?

```
chmod g+rx fast.c
chmod u+rw fast.c
chmod ug+rw fast.c
chmod u=rwx, g=rw, o=rx fast.c
```

> 3



<br>

### 9. 다음 중 a.txt의 마지막 10라인을 b.txt에 저장하는 shell 명령어로 알맞은 것은?

```
head < a.txt > b.txt
head > a.txt < b.txt
tail < a.txt > b.txt
tail > a.txt < b.txt
```

> 3



<br>

### 10. 다음 중 fastcampus.txt 파일에서 computer 라는 문구가 들어간 문구를 대, 소문자 구분 없이 검색해서 출력하는 shell 명령어로 알맞은 것은?

```
grep computer fastcampus.txt
grep -n computer fastcampus.txt
grep -r computer fastcampus.txt
grep -i computer fastcampus.txt
```

> 4



<br>

### **11. 다음 중 foreground 프로세스를 실행 중지하는 명령어로 알맞은 것은?**

```
CTRL + Z
CTRL + X
CTRL + C
CTRL + V
```

> 1
>
> ctrl z 가 실행중지고
>
> ctrl c는 프로세스 완전 종료 아님?! ><



<br>

### 12. 다음 중 시스템이 사용하고 있는 모든 프로세스의 상태를 확인하는 명령어로 알맞은 것은?

```
ps -a
ps -u
ps -l
ps -x
```

> 1



<br>

### 13. 다음 중 리눅스의 root directory에 포함되어 있지 않은 디렉토리는?

```
bin
opt
etc
include
```

> 4



<br>

### **14. 다음 그림을 보고, Soft Link에 대한 설명으로 알맞지 않은 것은?**

```
soft link에 있는 inode 값은 다르다.
soft link에 연결된 inode 구조에는 원본의 주소가 있다.
원본 파일이 사라지면, soft link는 해당 파일을 접근하지 못한다.
원본 파일이 수정되면, soft link는 해당 파일을 수정된 형태를 볼 수 있다.
```

> 2
>
> 원본 주소로 리다이렉트 한댔는데..
>
> 2도 맞는거 같은디 ;ㅂ;.. 머징... 
>
> ㅇㅈ  1, 3, 4가 명확하니까 2로..

<br>

### 15. 다음 중 시스템콜 함수가 아닌것은?

```
read()
write()
open()
take()
```

> 4



<br>

### 16. 다음 중 소프트웨어 인터럽트 명령에 해당하는 instruction value는?

```
0x20
0x40
0x60
0x80
```

> 4



<br>

### 17. 다음 중 시스템 콜의 구현 방법에 대한 설명으로 옳지 않은 것은?

```
CPU는 사용자 모드를 커널 모드로 바꿔준다.
IDT에서 0x80에 해당하는 주소를 찾아서 실행한다.
system_call() 함수에서 ebx로 부터 시스템 콜 번호를 찾아서, 해당 번호에 맞는 시스템 콜 함수를 불러온다.
해당 시스템 콜 함수를 실행 한 후, 커널 모드에서 사용자 모드로 변경하고 해당 프로세스의 다음 작업을 수행한다.
```

> 3



<br>

### 18. 다음 중 리눅스 C 컴파일러를 이용하여 test.c 파일을 컴파일 하여 test 라는 실행파일을 만드는 명령어는?

```
gcc test.c
gcc -o test.c test
gcc -o test test.c
gcc -c test.c test
```

> 2



<br>

### 19. 다음 중 클라우드 컴퓨팅 서비스에 대해 알맞지 않은 것은?

```
로컬 서버를 미리 구축해놓고 사용하는 개념이다.
서버 환경을 미리 구축해 놓고, 바로 사용할 수 있도록 제공한다.
대표적인 서비스로 Amazon Web Service(AWS)가 있다.
네트워크를 통해 데이터를 요청하고 받는 형식으로 진행된다.
```

> 1



<br>

### 20. 다음 중 AWS에 대해서 알맞지 않은 것은?

```
EC2를 사용해서 Computing 자원을 사용할 수 있다.
Elastic IP를 사용하여 적절한 IP주소를 필요할 때 할당받는다.
EC2를 사용할 Server Region을 직접 설정할 수 있다.
사용량과 관계없이 고정 비용이 청구된다.
```

> 4



<br>

### 21. 다음 중 운영체제를 구성하는 요소가 아닌 것은?

```
File Manager
Shell
Kernel
Application
```

> 1



<br>

### 22. 다음 중 현재 디렉토리에 있는 모든 파일의 목록과 그에 대한 내용을 출력하는 shell 명령어로 알맞은 것은?

```
ls -w
ls -d
ls -al
ls -f
```

> 3



<br>

### 23. 다음 중 Chromedriver 파일에 소유자 읽기 권한을 제외한 모든 권한을 없애는 shell 명령어로 알맞은 것은?

```
chmod 777
chmod 555
chmod 400
chmod 700
```

> 3



<br>

### 24. 다음 중 anaconda 디렉토리에 있는 모든 파일을 지우는 리눅스 명령어로 알맞은 것은?

```
rm anaconda
rm -r anaconda
rm -rf anaconda
rm -rf *
```

> 3

<br>

### 25. 다음 중 anaconda 디렉토리 내부에 있는 하위 경로에 존재하는 파일까지 모두 포함하여 python 문구를 포함하고 있는 파일의 목록을 출력하는 명령어로 알맞은 것은?

```
ls | grep -r python anaconda
grep -n python anaconda
ls | grep python anaconda
grep -i python anaconda
```

> 1

<br>

### 26. 다음 중 inference.py를 background에서 실행하는 명령어로 알맞은 것은?

```
python inference.py
python inference.py &
python & inference.py
find / -name 'inference.py' > list.txt &
```

> 2



<br>

### 27. background에서 실행중인 inference.py 프로세스를 확인해보니 pid가 100였다. 이 프로세스를 강제 종료하는 명령어로 알맞은 것은?

```
kill -5 100
kill -9 100
kill -100 9
kill 100
```

> 2



<br>

### 28. cp A B 명령어를 수행했을 때, 다음 설명 중 알맞지 않은 것은?

```
A와 B는 동일한 inode값을 가진다.
A와 B는 동일한 파일 크기를 갖는다.
A를 삭제하면 B에 접근이 불가능하다.
ls -al 명령어로 확인했을 때 출력 결과가 동일하다.
```

> 2
>
> 동의



<br>

### 29. GNU 프로젝트의 일환으로 개발된 리눅스 운영체제 대부분의 기본으로 설정되어 있는 shell의 이름은? (영문으로 작성해주세요)

```
bash
```





<br>

### **30. root directory가 현재 디렉토리 위치일 때, anaconda 폴더 안에 있는 envs 폴더안에 존재하는 모든 파일의 목록을 출력하는 shell 명령어를 작성하세요. (특수문자 포함, 띄어쓰기는 한 칸으로 통일합니다.)**

```
ls /anaconda/envs
```

